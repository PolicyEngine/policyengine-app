import PolicyEngine from "redesign/components/PolicyEngine.jsx";

jest.mock("react-plotly.js", () => jest.fn());

describe("Test cookie consent pop-up", () => {
  test("Test that pop-up appears on site load", () => {
  });

  test("Test that pop-up does not appear if cookies have been accepted", () => {

  });
});
describe("Test PoliciesModelledPopup", () => {
  test("Test that pop-up appears after beginning calculations", () => {
  });
});